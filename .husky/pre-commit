cd frontend && npx lint-staged

if pre-commit --version > /dev/null 2>&1; then
    echo "pre-commit detected to exist!"
    pre-commit run --all-files
else
    echo "pre-commit not installed, skipping."
fi
